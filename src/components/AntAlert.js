import 'antd/dist/antd.css';
import { Alert } from 'antd';

const AntAlert = () => (
  <Alert message="Error" description="Oops, something went wrong :-(" type="error" showIcon className="" />
);

export default AntAlert;
